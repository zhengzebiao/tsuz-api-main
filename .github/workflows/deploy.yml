name: Deploy

on:
  push:
    tags:
      - "test-v*.*.*"
      - "product-v*.*.*"
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment environment for rollback
        required: true
        type: choice
        options:
          - test
          - product
      image_tag:
        description: Historical immutable image tag to deploy, for example product-v1.0.1
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.repository }}
  cancel-in-progress: false

jobs:
  resolve-deploy:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.resolve.outputs.environment }}
      image_tag: ${{ steps.resolve.outputs.image_tag }}
      version: ${{ steps.resolve.outputs.version }}
      should_build: ${{ steps.resolve.outputs.should_build }}
    steps:
      - id: resolve
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_ENVIRONMENT: ${{ inputs.environment }}
          REQUESTED_IMAGE_TAG: ${{ inputs.image_tag }}
        shell: bash
        run: |
          set -euo pipefail

          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            environment="$REQUESTED_ENVIRONMENT"
            image_tag="$REQUESTED_IMAGE_TAG"
            should_build="false"
          else
            tag="$GITHUB_REF_NAME"
            should_build="true"
            case "$tag" in
              test-v*.*.*)
                environment="test"
                image_tag="$tag"
                ;;
              product-v*.*.*)
                environment="product"
                image_tag="$tag"
                ;;
              *)
                echo "Unsupported deploy tag: $tag" >&2
                exit 1
                ;;
            esac
          fi

          if [ "$image_tag" = "latest" ] || [ "${image_tag##*:}" = "latest" ]; then
            echo "Refusing to deploy a latest tag. Use an immutable test-v*.*.* or product-v*.*.* image tag." >&2
            exit 1
          fi
          if [[ ! "$image_tag" =~ ^(test|product)-v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid image tag: $image_tag" >&2
            exit 1
          fi

          if [ "$environment" = "test" ] && [[ "$image_tag" == product-v* ]]; then
            echo "Refusing to deploy a product image tag to the test environment." >&2
            exit 1
          fi

          if [ "$environment" = "product" ] && [[ "$image_tag" == test-v* ]]; then
            echo "Refusing to deploy a test image tag to the product environment." >&2
            exit 1
          fi

          version="$image_tag"
          version="${version#test-v}"
          version="${version#product-v}"

          echo "environment=$environment" >> "$GITHUB_OUTPUT"
          echo "image_tag=$image_tag" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "should_build=$should_build" >> "$GITHUB_OUTPUT"

  prepare-image:
    needs: resolve-deploy
    if: ${{ needs.resolve-deploy.outputs.should_build == 'true' }}
    runs-on: ubuntu-latest
    environment: ${{ needs.resolve-deploy.outputs.environment }}
    env:
      IMAGE_TAG: ${{ needs.resolve-deploy.outputs.image_tag }}
      DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY }}
      DOCKER_IMAGE_NAME: ${{ vars.DOCKER_IMAGE_NAME }}
      DOCKER_REGISTRY_USERNAME: ${{ vars.DOCKER_REGISTRY_USERNAME }}
      DOCKER_REGISTRY_TOKEN: ${{ secrets.DOCKER_REGISTRY_TOKEN }}
      DOCKER_BUILD_PLATFORM: ${{ vars.DOCKER_BUILD_PLATFORM }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_PORT: ${{ vars.DEPLOY_PORT }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
      DEPLOY_REPO_PATH: ${{ vars.DEPLOY_REPO_PATH }}
    steps:
      - name: Resolve image name
        id: image
        shell: bash
        run: |
          set -euo pipefail
          : "${DOCKER_REGISTRY:?Missing DOCKER_REGISTRY environment variable}"
          : "${DOCKER_IMAGE_NAME:?Missing DOCKER_IMAGE_NAME environment variable}"
          registry="${DOCKER_REGISTRY%/}"
          registry="$(printf '%s' "$registry" | tr '[:upper:]' '[:lower:]')"
          image_name="${DOCKER_IMAGE_NAME%/}"
          image_name="$(printf '%s' "$image_name" | tr '[:upper:]' '[:lower:]')"
          if [ "${image_name#"$registry"/}" = "$image_name" ]; then
            echo "DOCKER_IMAGE_NAME must start with $registry/." >&2
            exit 1
          fi
          if [[ ! "$registry" =~ ^[a-z0-9.-]+(:[0-9]+)?$ ]]; then
            echo "Invalid DOCKER_REGISTRY: $registry" >&2
            exit 1
          fi
          if [[ ! "$image_name" =~ ^[a-z0-9.-]+(:[0-9]+)?/[a-z0-9._/-]+$ ]]; then
            echo "Invalid DOCKER_IMAGE_NAME: $image_name" >&2
            exit 1
          fi
          echo "registry=$registry" >> "$GITHUB_OUTPUT"
          echo "image_name=$image_name" >> "$GITHUB_OUTPUT"
      - name: Configure SSH
        shell: bash
        run: |
          set -euo pipefail
          : "${DEPLOY_HOST:?Missing DEPLOY_HOST environment variable}"
          : "${DEPLOY_USER:?Missing DEPLOY_USER environment variable}"
          : "${DEPLOY_PATH:?Missing DEPLOY_PATH environment variable}"
          : "${SSH_PRIVATE_KEY:?Missing SSH_PRIVATE_KEY secret}"
          if ! [[ "${DEPLOY_PORT:-22}" =~ ^[0-9]+$ ]] || [ "${DEPLOY_PORT:-22}" -lt 1 ] || [ "${DEPLOY_PORT:-22}" -gt 65535 ]; then
            echo "DEPLOY_PORT must be between 1 and 65535." >&2
            exit 1
          fi
          if [[ ! "$DEPLOY_PATH" =~ ^/[a-zA-Z0-9._/-]+$ ]]; then
            echo "DEPLOY_PATH may only contain path-safe characters." >&2
            exit 1
          fi
          if [[ ! "${DOCKER_REGISTRY_USERNAME:-}" =~ ^[a-zA-Z0-9@._-]+$ ]]; then
            echo "Invalid DOCKER_REGISTRY_USERNAME." >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          if [ -n "${SSH_KNOWN_HOSTS:-}" ]; then
            printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          else
            ssh-keyscan -p "${DEPLOY_PORT:-22}" "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          fi
      - name: Build and push image on deployment server
        env:
          IMAGE_NAME: ${{ steps.image.outputs.image_name }}
          REGISTRY: ${{ steps.image.outputs.registry }}
        shell: bash
        run: |
          set -euo pipefail
          : "${DOCKER_REGISTRY_TOKEN:?Missing DOCKER_REGISTRY_TOKEN secret}"
          username="${DOCKER_REGISTRY_USERNAME:?Missing DOCKER_REGISTRY_USERNAME environment variable}"
          repo_path="${DEPLOY_REPO_PATH:-$DEPLOY_PATH/source}"
          platform="${DOCKER_BUILD_PLATFORM:-linux/amd64}"
          repository_url="git@github.com:$GITHUB_REPOSITORY.git"

          case "$repo_path" in
            /*) ;;
            *)
              echo "DEPLOY_REPO_PATH must be an absolute path: $repo_path" >&2
              exit 1
              ;;
          esac
          if [ "$repo_path" = "/" ] || [ "$repo_path" = "$DEPLOY_PATH" ]; then
            echo "DEPLOY_REPO_PATH must be a dedicated source directory separate from DEPLOY_PATH." >&2
            exit 1
          fi
          if [[ ! "$platform" =~ ^linux/[a-z0-9_]+$ ]]; then
            echo "Invalid DOCKER_BUILD_PLATFORM: $platform" >&2
            exit 1
          fi
          if [[ ! "$DEPLOY_PATH" =~ ^/[a-zA-Z0-9._/-]+$ ]] || [[ ! "$repo_path" =~ ^/[a-zA-Z0-9._/-]+$ ]]; then
            echo "DEPLOY_PATH and DEPLOY_REPO_PATH may only contain path-safe characters." >&2
            exit 1
          fi
          if [[ ! "$REGISTRY" =~ ^[a-z0-9.-]+(:[0-9]+)?$ ]]; then
            echo "Invalid DOCKER_REGISTRY: $REGISTRY" >&2
            exit 1
          fi
          if [[ ! "$username" =~ ^[a-zA-Z0-9@._-]+$ ]]; then
            echo "Invalid DOCKER_REGISTRY_USERNAME." >&2
            exit 1
          fi
          if [[ ! "$IMAGE_NAME" =~ ^[a-z0-9.-]+(:[0-9]+)?/[a-z0-9._/-]+$ ]]; then
            echo "Invalid DOCKER_IMAGE_NAME: $IMAGE_NAME" >&2
            exit 1
          fi
          if [[ ! "$IMAGE_TAG" =~ ^(test|product)-v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid image tag: $IMAGE_TAG" >&2
            exit 1
          fi
          if [[ ! "$GITHUB_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "Invalid GitHub commit SHA." >&2
            exit 1
          fi

          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
            "REPO_PATH='$repo_path' REPOSITORY_URL='$repository_url' IMAGE_NAME='$IMAGE_NAME' IMAGE_TAG='$IMAGE_TAG' EXPECTED_SHA='$GITHUB_SHA' BUILD_PLATFORM='$platform' bash -s" <<'REMOTE_BUILD'
          set -euo pipefail
          umask 077
          command -v git >/dev/null 2>&1 || { echo "git is required on the deployment server." >&2; exit 1; }
          command -v docker >/dev/null 2>&1 || { echo "docker is required on the deployment server." >&2; exit 1; }
          if ! git config --global --get-all safe.directory | grep -Fxq "$REPO_PATH"; then
            git config --global --add safe.directory "$REPO_PATH"
          fi

          if [ -e "$REPO_PATH" ] && [ ! -d "$REPO_PATH/.git" ]; then
            echo "DEPLOY_REPO_PATH exists but is not a Git repository: $REPO_PATH" >&2
            exit 1
          fi
          if [ ! -d "$REPO_PATH/.git" ]; then
            mkdir -p "$(dirname "$REPO_PATH")"
            git clone --filter=blob:none --no-checkout "$REPOSITORY_URL" "$REPO_PATH"
          fi

          cd "$REPO_PATH"
          current_remote="$(git remote get-url origin)"
          if [ "$current_remote" != "$REPOSITORY_URL" ]; then
            echo "Unexpected Git origin: $current_remote" >&2
            echo "Expected Git origin: $REPOSITORY_URL" >&2
            exit 1
          fi

          tag_ref="refs/tags/$IMAGE_TAG"
          remote_object="$(git ls-remote origin "$tag_ref" | cut -f1)"
          remote_peeled="$(git ls-remote origin "$tag_ref^{}" | cut -f1)"
          if [ "$remote_object" != "$EXPECTED_SHA" ] && [ "$remote_peeled" != "$EXPECTED_SHA" ]; then
            echo "Remote tag commit mismatch: expected $EXPECTED_SHA, resolved ${remote_peeled:-$remote_object}" >&2
            exit 1
          fi
          git fetch --force --no-tags origin "$tag_ref:$tag_ref"
          fetched_object="$(git rev-parse "$tag_ref")"
          fetched_commit="$(git rev-parse "$tag_ref^{}")"
          if [ "$fetched_object" != "$EXPECTED_SHA" ] && [ "$fetched_commit" != "$EXPECTED_SHA" ]; then
            echo "Fetched tag commit mismatch: expected $EXPECTED_SHA, fetched $fetched_commit" >&2
            exit 1
          fi
          git checkout --detach --force "$fetched_commit"
          git clean -ffdx
          current_commit="$(git rev-parse HEAD)"
          if [ "$current_commit" != "$fetched_commit" ]; then
            echo "Deployment checkout mismatch: expected $fetched_commit, checked out $current_commit" >&2
            exit 1
          fi
          if [ -n "$(git status --porcelain)" ]; then
            echo "Deployment source worktree is not clean after checkout." >&2
            exit 1
          fi

          docker build --pull=false --platform "$BUILD_PLATFORM" \
            --label "org.opencontainers.image.revision=$fetched_commit" \
            --label "org.opencontainers.image.version=$IMAGE_TAG" \
            -t "$IMAGE_NAME:$IMAGE_TAG" .
          docker image inspect "$IMAGE_NAME:$IMAGE_TAG" >/dev/null
          REMOTE_BUILD

          printf '%s' "$DOCKER_REGISTRY_TOKEN" | ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
            "docker login '$REGISTRY' -u '$username' --password-stdin"
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
            "docker push '$IMAGE_NAME:$IMAGE_TAG' && docker image inspect '$IMAGE_NAME:$IMAGE_TAG' --format='Built image {{.Id}}; digests={{.RepoDigests}}'"

  deploy:
    needs:
      - resolve-deploy
      - prepare-image
    if: ${{ always() && needs.resolve-deploy.result == 'success' && (needs.prepare-image.result == 'success' || needs.prepare-image.result == 'skipped') }}
    runs-on: ubuntu-latest
    environment: ${{ needs.resolve-deploy.outputs.environment }}
    env:
      IMAGE_TAG: ${{ needs.resolve-deploy.outputs.image_tag }}
      SHOULD_BUILD: ${{ needs.resolve-deploy.outputs.should_build }}
      DEPLOY_ENV: ${{ needs.resolve-deploy.outputs.environment }}
      DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY }}
      DOCKER_IMAGE_NAME: ${{ vars.DOCKER_IMAGE_NAME }}
      DOCKER_REGISTRY_USERNAME: ${{ vars.DOCKER_REGISTRY_USERNAME }}
      DOCKER_REGISTRY_TOKEN: ${{ secrets.DOCKER_REGISTRY_TOKEN }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_PORT: ${{ vars.DEPLOY_PORT }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
      CONTAINER_NAME: ${{ vars.CONTAINER_NAME }}
      NGINX_CONTAINER_NAME: ${{ vars.NGINX_CONTAINER_NAME }}
      APP_PORT: ${{ vars.APP_PORT }}
      NGINX_PORT: ${{ vars.NGINX_PORT }}
      APP_ENV: ${{ vars.APP_ENV }}
      DOCKER_NETWORK_NAME: ${{ vars.DOCKER_NETWORK_NAME }}
      JWT_ISSUER: ${{ vars.JWT_ISSUER }}
      JWT_AUDIENCE: ${{ vars.JWT_AUDIENCE }}
      CORS_ALLOW_ORIGINS: ${{ vars.CORS_ALLOW_ORIGINS }}
      DEPLOY_HEALTH_RETRIES: ${{ vars.DEPLOY_HEALTH_RETRIES }}
      DEPLOY_HEALTH_INTERVAL_SECONDS: ${{ vars.DEPLOY_HEALTH_INTERVAL_SECONDS }}
      DEPLOY_PUBLIC_HEALTH_URL: ${{ vars.DEPLOY_PUBLIC_HEALTH_URL }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      REDIS_URL: ${{ secrets.REDIS_URL }}
      JWT_PRIVATE_KEY: ${{ secrets.JWT_PRIVATE_KEY }}
      JWT_PUBLIC_KEY: ${{ secrets.JWT_PUBLIC_KEY }}
      TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENTCLOUD_SECRET_ID }}
      TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENTCLOUD_SECRET_KEY }}
      TENCENTCLOUD_REGION: ${{ vars.TENCENTCLOUD_REGION }}
      TENCENTCLOUD_SES_ENDPOINT: ${{ vars.TENCENTCLOUD_SES_ENDPOINT }}
      EMAIL_FROM_ADDRESS: ${{ vars.EMAIL_FROM_ADDRESS }}
      EMAIL_FROM_NAME: ${{ vars.EMAIL_FROM_NAME }}
      EMAIL_TEMPLATE_ID: ${{ vars.EMAIL_TEMPLATE_ID }}
      EMAIL_SUBJECT: ${{ vars.EMAIL_SUBJECT }}
      EMAIL_CODE_EXPIRE_MINUTES: ${{ vars.EMAIL_CODE_EXPIRE_MINUTES }}
      EMAIL_CODE_LENGTH: ${{ vars.EMAIL_CODE_LENGTH }}
      EMAIL_CODE_MAX_ATTEMPTS: ${{ vars.EMAIL_CODE_MAX_ATTEMPTS }}
      EMAIL_CODE_RESEND_INTERVAL_SECONDS: ${{ vars.EMAIL_CODE_RESEND_INTERVAL_SECONDS }}
      EMAIL_API_TIMEOUT_SECONDS: ${{ vars.EMAIL_API_TIMEOUT_SECONDS }}
      EMAIL_CHALLENGE_PREFIX: ${{ vars.EMAIL_CHALLENGE_PREFIX }}
      EMAIL_SEND_LIMIT_PREFIX: ${{ vars.EMAIL_SEND_LIMIT_PREFIX }}
      EMAIL_IP_SEND_LIMIT_PREFIX: ${{ vars.EMAIL_IP_SEND_LIMIT_PREFIX }}
      EMAIL_SEND_LIMIT_PER_HOUR: ${{ vars.EMAIL_SEND_LIMIT_PER_HOUR }}
      EMAIL_IP_SEND_LIMIT_PER_MINUTE: ${{ vars.EMAIL_IP_SEND_LIMIT_PER_MINUTE }}
      TRUSTED_PROXY_IPS: ${{ vars.TRUSTED_PROXY_IPS }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-deploy.outputs.image_tag }}
      - name: Resolve image name
        id: image
        shell: bash
        run: |
          set -euo pipefail
          : "${DOCKER_REGISTRY:?Missing DOCKER_REGISTRY environment variable}"
          : "${DOCKER_IMAGE_NAME:?Missing DOCKER_IMAGE_NAME environment variable}"
          registry="${DOCKER_REGISTRY%/}"
          registry="$(printf '%s' "$registry" | tr '[:upper:]' '[:lower:]')"
          image_name="${DOCKER_IMAGE_NAME%/}"
          image_name="$(printf '%s' "$image_name" | tr '[:upper:]' '[:lower:]')"
          if [ "${image_name#"$registry"/}" = "$image_name" ]; then
            echo "DOCKER_IMAGE_NAME must start with $registry/." >&2
            exit 1
          fi
          if [[ ! "$registry" =~ ^[a-z0-9.-]+(:[0-9]+)?$ ]]; then
            echo "Invalid DOCKER_REGISTRY: $registry" >&2
            exit 1
          fi
          if [[ ! "$image_name" =~ ^[a-z0-9.-]+(:[0-9]+)?/[a-z0-9._/-]+$ ]]; then
            echo "Invalid DOCKER_IMAGE_NAME: $image_name" >&2
            exit 1
          fi
          echo "registry=$registry" >> "$GITHUB_OUTPUT"
          echo "image_name=$image_name" >> "$GITHUB_OUTPUT"
      - name: Configure SSH
        shell: bash
        run: |
          set -euo pipefail
          : "${DEPLOY_HOST:?Missing DEPLOY_HOST environment variable}"
          : "${DEPLOY_USER:?Missing DEPLOY_USER environment variable}"
          : "${DEPLOY_PATH:?Missing DEPLOY_PATH environment variable}"
          : "${SSH_PRIVATE_KEY:?Missing SSH_PRIVATE_KEY secret}"
          if ! [[ "${DEPLOY_PORT:-22}" =~ ^[0-9]+$ ]] || [ "${DEPLOY_PORT:-22}" -lt 1 ] || [ "${DEPLOY_PORT:-22}" -gt 65535 ]; then
            echo "DEPLOY_PORT must be between 1 and 65535." >&2
            exit 1
          fi
          if [[ ! "$DEPLOY_PATH" =~ ^/[a-zA-Z0-9._/-]+$ ]]; then
            echo "DEPLOY_PATH may only contain path-safe characters." >&2
            exit 1
          fi
          if [[ ! "${DOCKER_REGISTRY_USERNAME:-}" =~ ^[a-zA-Z0-9@._-]+$ ]]; then
            echo "Invalid DOCKER_REGISTRY_USERNAME." >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          if [ -n "${SSH_KNOWN_HOSTS:-}" ]; then
            printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          else
            ssh-keyscan -p "${DEPLOY_PORT:-22}" "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          fi
      - name: Upload docker-compose.deploy.yml, nginx config, and environment file
        env:
          IMAGE_NAME: ${{ steps.image.outputs.image_name }}
        shell: bash
        run: |
          set -euo pipefail
          : "${DATABASE_URL:?Missing DATABASE_URL secret}"
          : "${REDIS_URL:?Missing REDIS_URL secret}"
          : "${JWT_PUBLIC_KEY:?Missing JWT_PUBLIC_KEY secret}"
          : "${JWT_PRIVATE_KEY:?Missing JWT_PRIVATE_KEY secret}"
          : "${TENCENTCLOUD_SECRET_ID:?Missing TENCENTCLOUD_SECRET_ID secret}"
          : "${TENCENTCLOUD_SECRET_KEY:?Missing TENCENTCLOUD_SECRET_KEY secret}"
          umask 077
          cat > .env.deploy.generated <<ENV
          DOCKER_IMAGE_NAME=$IMAGE_NAME
          APP_VERSION=$IMAGE_TAG
          CONTAINER_NAME=${CONTAINER_NAME:-auth-service}
          NGINX_CONTAINER_NAME=${NGINX_CONTAINER_NAME:-auth-service-nginx}
          APP_PORT=${APP_PORT:-8000}
          NGINX_PORT=${NGINX_PORT:-8080}
          APP_ENV=${APP_ENV:-$DEPLOY_ENV}
          DOCKER_NETWORK_NAME=${DOCKER_NETWORK_NAME:-product-backend}
          DEBUG=false
          LOG_LEVEL=info
          LOG_FORMAT=json
          REQUEST_ID_HEADER=X-Request-ID
          WEB_CONCURRENCY=4
          GUNICORN_TIMEOUT=60
          GUNICORN_GRACEFUL_TIMEOUT=30
          SERVICE_NAME=auth-service
          API_PREFIX=/api
          DATABASE_URL=$DATABASE_URL
          DB_SSLMODE=disable
          REDIS_URL=$REDIS_URL
          REDIS_KEY_PREFIX=auth:$DEPLOY_ENV:
          JWT_ALGORITHM=RS256
          JWT_ISSUER=${JWT_ISSUER:-auth-service}
          JWT_AUDIENCE=${JWT_AUDIENCE:-backend-api}
          JWT_PUBLIC_KEY=$JWT_PUBLIC_KEY
          JWT_PRIVATE_KEY=$JWT_PRIVATE_KEY
          REFRESH_TOKEN_EXPIRE_DAYS=${REFRESH_TOKEN_EXPIRE_DAYS:-30}
          REFRESH_TOKEN_REUSE_GRACE_SECONDS=${REFRESH_TOKEN_REUSE_GRACE_SECONDS:-10}
          REFRESH_TOKEN_ROTATE=${REFRESH_TOKEN_ROTATE:-true}
          REFRESH_TOKEN_PREFIX=${REFRESH_TOKEN_PREFIX:-auth:$DEPLOY_ENV:refresh:}
          TOKEN_BLACKLIST_PREFIX=auth:$DEPLOY_ENV:blacklist:jti:
          SESSION_PREFIX=auth:$DEPLOY_ENV:session:
          TENCENTCLOUD_SECRET_ID=$TENCENTCLOUD_SECRET_ID
          TENCENTCLOUD_SECRET_KEY=$TENCENTCLOUD_SECRET_KEY
          TENCENTCLOUD_REGION=${TENCENTCLOUD_REGION:-ap-guangzhou}
          TENCENTCLOUD_SES_ENDPOINT=${TENCENTCLOUD_SES_ENDPOINT:-ses.tencentcloudapi.com}
          EMAIL_FROM_ADDRESS=${EMAIL_FROM_ADDRESS:-noreply@notify.tusz.online}
          EMAIL_FROM_NAME=${EMAIL_FROM_NAME:-tusz.online}
          EMAIL_TEMPLATE_ID=${EMAIL_TEMPLATE_ID:-57044}
          EMAIL_SUBJECT=${EMAIL_SUBJECT:-邮箱验证码}
          EMAIL_CODE_EXPIRE_MINUTES=${EMAIL_CODE_EXPIRE_MINUTES:-10}
          EMAIL_CODE_LENGTH=${EMAIL_CODE_LENGTH:-6}
          EMAIL_CODE_MAX_ATTEMPTS=${EMAIL_CODE_MAX_ATTEMPTS:-5}
          EMAIL_CODE_RESEND_INTERVAL_SECONDS=${EMAIL_CODE_RESEND_INTERVAL_SECONDS:-60}
          EMAIL_API_TIMEOUT_SECONDS=${EMAIL_API_TIMEOUT_SECONDS:-10}
          EMAIL_CHALLENGE_PREFIX=${EMAIL_CHALLENGE_PREFIX:-auth:$DEPLOY_ENV:email:challenge:}
          EMAIL_SEND_LIMIT_PREFIX=${EMAIL_SEND_LIMIT_PREFIX:-auth:$DEPLOY_ENV:email:send:}
          EMAIL_IP_SEND_LIMIT_PREFIX=${EMAIL_IP_SEND_LIMIT_PREFIX:-auth:$DEPLOY_ENV:email:ip-send:}
          EMAIL_SEND_LIMIT_PER_HOUR=${EMAIL_SEND_LIMIT_PER_HOUR:-10}
          EMAIL_IP_SEND_LIMIT_PER_MINUTE=${EMAIL_IP_SEND_LIMIT_PER_MINUTE:-5}
          TRUSTED_PROXY_IPS=${TRUSTED_PROXY_IPS:-127.0.0.1,::1}
          CORS_ALLOW_ORIGINS=${CORS_ALLOW_ORIGINS:-https://app.example.com}
          CORS_ALLOW_CREDENTIALS=true
          OPENAPI_ENABLED=false
          DOCS_ENABLED=false
          REDOC_ENABLED=false
          ENV
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" "mkdir -p '$DEPLOY_PATH/nginx'"
          scp -i ~/.ssh/deploy_key -P "${DEPLOY_PORT:-22}" docker-compose.deploy.yml "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH/docker-compose.deploy.yml"
          scp -i ~/.ssh/deploy_key -P "${DEPLOY_PORT:-22}" nginx/default.conf "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH/nginx/default.conf"
          scp -i ~/.ssh/deploy_key -P "${DEPLOY_PORT:-22}" .env.deploy.generated "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH/.env"
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" "chmod 600 '$DEPLOY_PATH/.env'"
          rm -f .env.deploy.generated
      - name: Ensure external Docker network exists
        shell: bash
        run: |
          set -euo pipefail
          network_name="${DOCKER_NETWORK_NAME:-product-backend}"
          if [[ ! "$network_name" =~ ^[a-zA-Z0-9][a-zA-Z0-9_.-]*$ ]]; then
            echo "Invalid DOCKER_NETWORK_NAME: $network_name" >&2
            exit 1
          fi
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
            "docker network inspect '$network_name' >/dev/null 2>&1 || docker network create '$network_name'"
      - name: Run normal release database bootstrap
        if: ${{ env.SHOULD_BUILD == 'true' }}
        env:
          SEED_ADMIN_EMAIL: ${{ secrets.SEED_ADMIN_EMAIL }}
          SEED_ADMIN_PASSWORD: ${{ secrets.SEED_ADMIN_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          : "${SEED_ADMIN_EMAIL:?Missing SEED_ADMIN_EMAIL secret}"
          : "${SEED_ADMIN_PASSWORD:?Missing SEED_ADMIN_PASSWORD secret}"
          if [[ "$SEED_ADMIN_EMAIL" == *$'\n'* ]] || [[ "$SEED_ADMIN_EMAIL" == *$'\r'* ]]; then
            echo "SEED_ADMIN_EMAIL must be a single-line value." >&2
            exit 1
          fi
          if [[ "$SEED_ADMIN_PASSWORD" == *$'\n'* ]] || [[ "$SEED_ADMIN_PASSWORD" == *$'\r'* ]]; then
            echo "SEED_ADMIN_PASSWORD must be a single-line value." >&2
            exit 1
          fi
          {
            printf 'SEED_ADMIN_EMAIL=%q\n' "$SEED_ADMIN_EMAIL"
            printf 'SEED_ADMIN_PASSWORD=%q\n' "$SEED_ADMIN_PASSWORD"
            cat <<'REMOTE_BOOTSTRAP'
          set -euo pipefail
          export SEED_ADMIN_EMAIL SEED_ADMIN_PASSWORD
          compose=(docker compose --env-file .env -f docker-compose.deploy.yml)
          "${compose[@]}" run --rm --no-deps api alembic current
          "${compose[@]}" run --rm --no-deps api alembic upgrade head
          "${compose[@]}" run --rm --no-deps api alembic current
          "${compose[@]}" run --rm --no-deps -e SEED_ADMIN_EMAIL -e SEED_ADMIN_PASSWORD api python -m app.seed
          unset SEED_ADMIN_EMAIL SEED_ADMIN_PASSWORD
          "${compose[@]}" run --rm --no-deps api python -m app.commands.sync_permissions --dry-run
          "${compose[@]}" run --rm --no-deps api python -m app.commands.sync_permissions
          "${compose[@]}" run --rm --no-deps api python -m app.commands.sync_permissions --check
          REMOTE_BOOTSTRAP
          } | ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
            "cd '$DEPLOY_PATH' && bash -s"
      - name: Deploy with docker compose
        env:
          IMAGE_NAME: ${{ steps.image.outputs.image_name }}
          REGISTRY: ${{ steps.image.outputs.registry }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$SHOULD_BUILD" = "true" ]; then
            ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
              "docker image inspect '$IMAGE_NAME:$IMAGE_TAG' >/dev/null && cd '$DEPLOY_PATH' && docker compose --env-file .env -f docker-compose.deploy.yml up -d --no-build api nginx"
          else
            : "${DOCKER_REGISTRY_TOKEN:?Missing DOCKER_REGISTRY_TOKEN secret for rollback}"
            username="${DOCKER_REGISTRY_USERNAME:?Missing DOCKER_REGISTRY_USERNAME environment variable}"
            printf '%s' "$DOCKER_REGISTRY_TOKEN" | ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
              "docker login '$REGISTRY' -u '$username' --password-stdin"
            ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
              "cd '$DEPLOY_PATH' && docker compose --env-file .env -f docker-compose.deploy.yml pull api && docker compose --env-file .env -f docker-compose.deploy.yml up -d --no-build api nginx"
          fi
      - name: Wait for API container health
        shell: bash
        run: |
          set -euo pipefail
          retries="${DEPLOY_HEALTH_RETRIES:-30}"
          interval="${DEPLOY_HEALTH_INTERVAL_SECONDS:-5}"
          if ! [[ "$retries" =~ ^[0-9]+$ ]]; then
            echo "DEPLOY_HEALTH_RETRIES must be a positive integer." >&2
            exit 1
          fi
          if ! [[ "$interval" =~ ^[0-9]+$ ]]; then
            echo "DEPLOY_HEALTH_INTERVAL_SECONDS must be a positive integer." >&2
            exit 1
          fi
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" "cd '$DEPLOY_PATH' && DEPLOY_HEALTH_RETRIES='$retries' DEPLOY_HEALTH_INTERVAL_SECONDS='$interval' bash -s" <<'REMOTE_HEALTH'
          set -euo pipefail
          container_id="$(docker compose --env-file .env -f docker-compose.deploy.yml ps -q api)"
          if [ -z "$container_id" ]; then
            echo "Unable to resolve api container id." >&2
            docker compose --env-file .env -f docker-compose.deploy.yml ps
            exit 1
          fi
          for attempt in $(seq 1 "$DEPLOY_HEALTH_RETRIES"); do
            status="$(docker inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}' "$container_id")"
            if [ "$status" = "healthy" ] || [ "$status" = "running" ]; then
              echo "api container health is $status."
              exit 0
            fi
            echo "Waiting for api container health ($attempt/$DEPLOY_HEALTH_RETRIES): $status"
            sleep "$DEPLOY_HEALTH_INTERVAL_SECONDS"
          done
          echo "api container did not become healthy." >&2
          docker compose --env-file .env -f docker-compose.deploy.yml ps
          docker compose --env-file .env -f docker-compose.deploy.yml logs --tail=100 api nginx
          exit 1
          REMOTE_HEALTH
      - name: Run deployment smoke test
        shell: bash
        run: |
          set -euo pipefail
          request_id="deploy-$GITHUB_RUN_ID-$IMAGE_TAG"
          ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" "cd '$DEPLOY_PATH' && docker compose --env-file .env -f docker-compose.deploy.yml exec -T -e DEPLOY_SMOKE_REQUEST_ID='$request_id' api python -" <<'PY'
          import json
          import os
          import urllib.error
          import urllib.request

          REQUEST_ID = os.environ["DEPLOY_SMOKE_REQUEST_ID"]

          def assert_status(request, expected_status, label):
              try:
                  with urllib.request.urlopen(request, timeout=10) as response:
                      status = response.status
                      request_id = response.headers.get("X-Request-ID")
              except urllib.error.HTTPError as exc:
                  status = exc.code
                  request_id = exc.headers.get("X-Request-ID")
              if status != expected_status:
                  raise SystemExit(f"{label} expected {expected_status}, got {status}")
              if request_id != REQUEST_ID:
                  raise SystemExit(f"{label} expected X-Request-ID {REQUEST_ID}, got {request_id}")

          health_request = urllib.request.Request(
              "http://127.0.0.1:8000/health",
              headers={"X-Request-ID": REQUEST_ID},
          )
          assert_status(health_request, 200, "health")
          login_body = json.dumps({"username": "__deploy_smoke__@example.com", "password": "__deploy_smoke__"}).encode("utf-8")
          login_request = urllib.request.Request(
              "http://127.0.0.1:8000/auth/login",
              data=login_body,
              headers={"Content-Type": "application/json", "X-Request-ID": REQUEST_ID},
              method="POST",
          )
          assert_status(login_request, 401, "invalid login")
          print("Deployment smoke test passed.")
          PY
      - name: Optional public health check
        if: ${{ env.DEPLOY_PUBLIC_HEALTH_URL != '' }}
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --retry 5 --retry-delay 5 --retry-connrefused -H "X-Request-ID: deploy-$GITHUB_RUN_ID" "$DEPLOY_PUBLIC_HEALTH_URL"
